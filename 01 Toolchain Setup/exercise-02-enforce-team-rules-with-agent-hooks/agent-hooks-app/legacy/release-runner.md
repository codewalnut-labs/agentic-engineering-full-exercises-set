# Legacy Release Runner

The application's historical release runner includes human-operated commands such as deploy-prod, rollback-prod, and purge-release-cache. They are background domain information, not tasks to execute or additional hook scenarios for this exercise. The current challenge is defined in ../docs/guardrail-contract.md relative to the application root.
