diff --git a/11 Agentic Refactoring/exercise-01-characterization-test-refactor/evidence/before-output.json b/11 Agentic Refactoring/exercise-01-characterization-test-refactor/evidence/before-output.json
new file mode 100644
index 0000000000000000000000000000000000000000..65618e3349fdae391bea07b3f53d38525e6a803c
--- /dev/null
+++ b/11 Agentic Refactoring/exercise-01-characterization-test-refactor/evidence/before-output.json	
@@ -0,0 +1,142 @@
+[
+  {
+    "name": "enterprise boundary with one late payment",
+    "input": {
+      "tier": "enterprise",
+      "monthsActive": 12,
+      "latePayments": 1,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "eligible",
+      "discountPercent": 15,
+      "reason": "enterprise-tenure"
+    }
+  },
+  {
+    "name": "enterprise boundary with two late payments",
+    "input": {
+      "tier": "enterprise",
+      "monthsActive": 12,
+      "latePayments": 2,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "manual-review",
+      "discountPercent": 0,
+      "reason": "payment-history"
+    }
+  },
+  {
+    "name": "enterprise below tenure boundary",
+    "input": {
+      "tier": "enterprise",
+      "monthsActive": 11,
+      "latePayments": 0,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "ineligible",
+      "discountPercent": 0,
+      "reason": "plan-not-supported"
+    }
+  },
+  {
+    "name": "enterprise accepts negative late payments",
+    "input": {
+      "tier": "enterprise",
+      "monthsActive": 12,
+      "latePayments": -1,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "eligible",
+      "discountPercent": 15,
+      "reason": "enterprise-tenure"
+    }
+  },
+  {
+    "name": "pro exact tenure clean account",
+    "input": {
+      "tier": "pro",
+      "monthsActive": 6,
+      "latePayments": 0,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "eligible",
+      "discountPercent": 10,
+      "reason": "pro-tenure"
+    }
+  },
+  {
+    "name": "pro below tenure boundary",
+    "input": {
+      "tier": "pro",
+      "monthsActive": 5,
+      "latePayments": 0,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "ineligible",
+      "discountPercent": 0,
+      "reason": "plan-not-supported"
+    }
+  },
+  {
+    "name": "mature pro with one late payment",
+    "input": {
+      "tier": "pro",
+      "monthsActive": 36,
+      "latePayments": 1,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "ineligible",
+      "discountPercent": 0,
+      "reason": "plan-not-supported"
+    }
+  },
+  {
+    "name": "support override wins on new starter",
+    "input": {
+      "tier": "starter",
+      "monthsActive": 0,
+      "latePayments": 4,
+      "supportOverride": true
+    },
+    "output": {
+      "status": "eligible",
+      "discountPercent": 0,
+      "reason": "legacy-support-override"
+    }
+  },
+  {
+    "name": "support override wins on enterprise arrears",
+    "input": {
+      "tier": "enterprise",
+      "monthsActive": 24,
+      "latePayments": 8,
+      "supportOverride": true
+    },
+    "output": {
+      "status": "eligible",
+      "discountPercent": 0,
+      "reason": "legacy-support-override"
+    }
+  },
+  {
+    "name": "ordinary starter remains unsupported",
+    "input": {
+      "tier": "starter",
+      "monthsActive": 18,
+      "latePayments": 0,
+      "supportOverride": false
+    },
+    "output": {
+      "status": "ineligible",
+      "discountPercent": 0,
+      "reason": "plan-not-supported"
+    }
+  }
+]
diff --git a/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.characterization.test.mjs b/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.characterization.test.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..0ad6943bd7daf93a8661fac887678b884795b0ef
--- /dev/null
+++ b/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.characterization.test.mjs	
@@ -0,0 +1,38 @@
+import assert from "node:assert/strict";
+import fs from "node:fs";
+import path from "node:path";
+import { evaluateRenewalEligibility } from "./legacyEligibility.mjs";
+
+// Characterization test: pins the observed public behavior of the legacy
+// renewal rule before any structural refactor. Every case below is an
+// observation loaded from docs/renewal-golden-cases.json, including the
+// surprising ones — support override precedence, negative late-payment
+// counts accepted, and plan-not-supported on otherwise mature accounts.
+// The test exercises only the public export evaluateRenewalEligibility
+// and asserts deep equality of the complete result objects, so status,
+// discountPercent, and the exact reason strings are all pinned. It is
+// committed before any production edit so the refactor has a safety net.
+const goldenCasesPath = path.resolve(import.meta.dirname, "..", "..", "..", "docs", "renewal-golden-cases.json");
+const cases = JSON.parse(fs.readFileSync(goldenCasesPath, "utf8"));
+
+if (!Array.isArray(cases) || cases.length !== 10) {
+  console.error(`expected 10 golden cases in renewal-golden-cases.json, found ${cases?.length}`);
+  process.exit(1);
+}
+
+let failures = 0;
+for (const item of cases) {
+  try {
+    assert.deepEqual(evaluateRenewalEligibility(item.input), item.expected);
+    console.log(`PASS ${item.name}`);
+  } catch (error) {
+    failures += 1;
+    console.error(`FAIL ${item.name}\n${error.message}`);
+  }
+}
+
+if (failures > 0) {
+  console.error(`${failures} characterization observation(s) no longer match the golden baseline`);
+  process.exit(1);
+}
+console.log(`PASS ${cases.length} characterization observations match the golden baseline`);
diff --git a/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.mjs b/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.mjs
index 14914eb4ee0601134335de1156baea32b081bbb4..ab1f3016a6b537a8d80230edc768c4f7e2720b37 100644
--- a/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.mjs	
+++ b/11 Agentic Refactoring/exercise-01-characterization-test-refactor/rules-refactor-app/src/rules/legacyEligibility.mjs	
@@ -1,25 +1,37 @@
+const RENEWAL_DECISIONS = [
+  {
+    when: (account) => account.supportOverride === true,
+    status: "eligible",
+    discountPercent: 0,
+    reason: "legacy-support-override",
+  },
+  {
+    when: (account) => account.tier === "enterprise" && account.monthsActive >= 12 && account.latePayments < 2,
+    status: "eligible",
+    discountPercent: 15,
+    reason: "enterprise-tenure",
+  },
+  {
+    when: (account) => account.tier === "enterprise" && account.monthsActive >= 12,
+    status: "manual-review",
+    discountPercent: 0,
+    reason: "payment-history",
+  },
+  {
+    when: (account) => account.tier === "pro" && account.monthsActive >= 6 && account.latePayments === 0,
+    status: "eligible",
+    discountPercent: 10,
+    reason: "pro-tenure",
+  },
+];
+
+const PLAN_NOT_SUPPORTED = { status: "ineligible", discountPercent: 0, reason: "plan-not-supported" };
+
 export function evaluateRenewalEligibility(account) {
-  let status = "ineligible";
-  let discountPercent = 0;
-  let reason = "plan-not-supported";
-  if (account.supportOverride === true) {
-    status = "eligible";
-    reason = "legacy-support-override";
-  } else if (account.tier === "enterprise") {
-    if (account.monthsActive >= 12) {
-      if (account.latePayments < 2) {
-        status = "eligible";
-        discountPercent = 15;
-        reason = "enterprise-tenure";
-      } else {
-        status = "manual-review";
-        reason = "payment-history";
-      }
+  for (const decision of RENEWAL_DECISIONS) {
+    if (decision.when(account)) {
+      return { status: decision.status, discountPercent: decision.discountPercent, reason: decision.reason };
     }
-  } else if (account.tier === "pro" && account.monthsActive >= 6 && account.latePayments === 0) {
-    status = "eligible";
-    discountPercent = 10;
-    reason = "pro-tenure";
   }
-  return { status, discountPercent, reason };
+  return { ...PLAN_NOT_SUPPORTED };
 }
