# Refactor Steps — Exercise 11.1

The characterization-first workflow, in the order it ran on branch `codex/exercise-11-01-characterization-test-refactor` from starting commit `52090edddf032d026ece16ef90feb627bf8e67ac`.

1. **Inspect before touching.** Read the legacy rule (`rules-refactor-app/src/rules/legacyEligibility.mjs`), `docs/legacy-behavior-notes.md`, and the ten golden observations in `docs/renewal-golden-cases.json`. Confirmed the starting implementation satisfies every observation: `node ./scripts/run-characterization-oracle.mjs` — exit code: 0.

2. **Characterization before production edits.** Wrote `src/rules/legacyEligibility.characterization.test.mjs`, which imports only the public export `evaluateRenewalEligibility`, replays all ten golden cases with `assert.deepEqual`, and fails if any observation drifts. Ran it against the untouched legacy code — exit code: 0 (all ten observations green before any edit). Captured the baseline snapshot with `node ./scripts/run-characterization-oracle.mjs --json > ../evidence/before-output.json` — exit code: 0, output SHA-256 `5ce2adedc6dcb063b8ba99dbb1a2bbd49755e3e92437548b806dcea56aa98bab`.

3. **Commit the safety net first.** Committed exactly the characterization test and `evidence/before-output.json` as `characterizationSha` `af05bfacf17d1b37ba99e50996cc5686f2772683` ("characterization: pin ten observed renewal behaviors") — no production file touched in that commit, verified with `git diff-tree --no-commit-id --name-only -r af05bfa`.

4. **Classify before refactoring.** Recorded every surprising behavior as preserve or suspected bug in `evidence/behavior-decisions.md` — support-override precedence, negative late-payment acceptance, `plan-not-supported` on mature pro accounts, tenure boundaries, the two-late-payment threshold, unknown-tier default, and missing-field tolerance. This challenge authorizes preserve only, so no behavior changed.

5. **Fresh-session refactor.** A fresh agent session (same agent, model, tools, permissions, request, and time limit as the unconstrained attempt, with the characterization-first contract as its only extra input) refactored only `src/rules/legacyEligibility.mjs`: the mutable-accumulator nested conditionals became an ordered first-match-wins decision table preserving the export, inputs, result fields, exact reason strings, validation gaps, and decision order. Committed as the single-file `refactorSha` `550e1a2e4f5254375fe6360771ad260d9c4d34c2`, verified with `git diff-tree --no-commit-id --name-only -r 550e1a2` — exactly one file.

6. **Prove parity after the refactor.** Re-ran the characterization test — exit code: 0. Re-ran the oracle — exit code: 0. Captured `node ./scripts/run-characterization-oracle.mjs --json > ../evidence/after-output.json` — exit code: 0 — and `cmp ../evidence/before-output.json ../evidence/after-output.json` reports byte-identical files (same SHA-256 `5ce2ade…`). An independent exhaustive sweep over 49,392 stable-property input variations (7 tiers × 14 tenure values × 14 late-payment values × 6 override values × 6 object shapes) found zero output divergences from the starting implementation; stateful-accessor inputs are analyzed separately in `evidence/commands/stateful-accessor-parity.txt` and `evidence/comparison.md`.

7. **Command proof.** Every gate was run individually with captured output and exit codes, recorded in `evidence/commands/` and `evidence/verification.md`, including `npm run refactor:verify` and `npm run verify:exercise` — exit code: 0. All commits after `refactorSha` touch `evidence/` only, which `npm run refactor:verify` enforces against Git history.
