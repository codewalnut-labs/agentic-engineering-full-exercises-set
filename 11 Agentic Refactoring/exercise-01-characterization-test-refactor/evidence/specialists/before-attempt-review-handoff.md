# Before-Attempt Review Handoff — Exercise 11.1

Lane: before-attempt review (read-only). Verdict: **defects found** — one substantive output-divergence class on exotic inputs, plus three harness blind spots, two of which are exactly what hid the divergence. Every plain-data input is byte-identical to the original. Verification command (`node ./scripts/run-characterization-oracle.mjs` in the before worktree) — exit code: 0. The lane also re-ran the attempt's own harness — exit code: 0 — and verified its embedded oracle is a faithful verbatim copy of the original implementation (0 divergences over 247,163 combinations).

## Findings and disposition

| # | Finding (citations per the lane, against `legacyEligibility.mjs` at `bf7b280` unless noted) | Disposition |
|---|---|---|
| 1 | Stateful-accessor output divergence: the rule table re-reads `tier` up to 3× and `monthsActive` up to 2× (`legacyEligibility.mjs:7-8`, `:12`, `:16-17` at `bf7b280`) where the original reads each at most once per branch; a stateful `monthsActive` getter (11 then 40) flips `plan-not-supported` to `manual-review`, a stateful `tier` getter (enterprise then pro) flips `plan-not-supported` to `eligible`/`pro-tenure`, and read-count deltas fire caller-visible getter side effects up to 3× on 6 of 9 paths | **Accepted — documented, no code action.** Re-derived by the integration owner with `/tmp/parity/stateful-repro.mjs` (captured in `evidence/commands/stateful-accessor-parity.txt`). The before attempt stands uncorrected per the README ("Do not correct or retry it"); the finding is recorded in `before.md`, `comparison.md`, and `integration.md`. |
| 2 | Harness blind spot hiding Finding 1: the input grid (`legacyEligibility.characterization.mjs:157-171` at `bf7b280`) constructs only plain data objects — no getters or Proxies — so all 17,687 comparisons stay green while the divergence class exists | **Accepted — documented** in `comparison.md` dimension 6 as part of the before-attempt review; no code action (the harness belongs to the recorded attempt). |
| 3 | The "sparse" grid variant is a mislabel: `Object.entries(full)` (`legacyEligibility.characterization.mjs:162-167`) copies keys with `undefined` values, so the sparse shape is structurally identical to full and adds no coverage; truly key-absent accounts enter only via two named cases | **Accepted — documented** in `comparison.md` dimension 6. Coverage-claim defect, not a behavior defect. |
| 4 | `compareOutcome` (`legacyEligibility.characterization.mjs:148-152`) compares only the three field values — blind to key order, extra properties, enumerability, prototype; throw checks compare only threw/didn't-throw, not error type or message | **Accepted — documented** in `comparison.md` dimension 6. The shipped refactor is in fact correct on each of those axes (the lane verified each independently), so this is a net-limitation finding, not a regression. |

## What the lane checked and found clean

Reason strings verbatim across a 247,163-combination differential; validation gaps preserved (negatives, `−Infinity` identical, pro strict `=== 0`); decision order preserved; public API identical (export set, name, arity, descriptors, prototype); key order `status, discountPercent, reason` byte-equal across all paths; no shared mutable result objects (spread copy at `legacyEligibility.mjs:26`); identical `TypeError` name+message for null/undefined accounts; the 17,687-comparison arithmetic re-derived; `before.patch` content-identical to `git diff 52090ed bf7b280` and scoped to the exercise; nothing protected touched.
