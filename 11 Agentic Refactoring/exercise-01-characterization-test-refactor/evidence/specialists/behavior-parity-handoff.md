# Behavior Parity Handoff — Exercise 11.1

Lane: behavior parity (read-only). Verdict: **defects found** — value-level parity is clean across every stable-property input (152,880 exotic-value combinations, zero divergences), but property-access semantics diverge on stateful-accessor inputs: one root cause (the decision table re-reads `tier` up to 3× and `monthsActive` up to 2× where the original's else-if cascade reads each at most once per branch), six demonstrated divergences, five of which change the returned result or throw behavior. Both verification commands (`node ./src/rules/legacyEligibility.characterization.test.mjs`, `node ./scripts/run-characterization-oracle.mjs`) — exit code: 0 each. Copy provenance verified: `/tmp/parity/after.mjs` is diff-identical to `550e1a2`'s module and `/tmp/parity/orig.mjs` to `52090ed`'s.

## Findings and disposition

| # | Finding (citations per the lane, `after.mjs`/`orig.mjs` line numbers applying to SHA `550e1a2` / `52090ed` respectively) | Disposition |
|---|---|---|
| 1 | Result divergence, tier getter `enterprise`→`pro`, months 6, late 0: original `plan-not-supported` (else-if chain never re-evaluates after entering the enterprise branch) vs refactored `eligible`/`pro-tenure` (`after.mjs:15` re-reads `tier`, `after.mjs:21` re-reads again) | **Accepted — documented, deferred.** Re-derived by the integration owner; recorded in `after.md`, `comparison.md`, `integration.md`; captured in `evidence/commands/stateful-accessor-parity.txt`. No post-hoc code change — see integration reasoning. |
| 2 | Result divergence, same tier getter on the manual-review path (months 12, late 5): original `manual-review`/`payment-history` vs refactored default `plan-not-supported` | **Accepted — documented, deferred** (same root cause, same disposition). |
| 3 | Result divergence, stateful `monthsActive` getter (12 then 3, enterprise, late 5): original `manual-review` vs refactored default | **Accepted — documented, deferred** (same root cause). |
| 4 | Result divergence, mutating getter (latePayments getter deletes `this.monthsActive`): original consumed `monthsActive` before the mutation; the table re-reads it afterwards and sees `undefined` | **Accepted — documented, deferred** (same root cause). |
| 5 | Result divergence, second-read tier `"enterprise"` (getter: starter, enterprise, …): the original's second read only compares `=== "pro"`; the table's second read compares `=== "enterprise"` with no latePayments guard and matches → `manual-review` where the original returns `plan-not-supported` | **Accepted — documented, deferred** (same root cause). |
| 6 | Exception divergence: a Proxy whose get trap throws on the second read of any property returns normally under the original and throws under the refactor | **Accepted — documented, deferred** — reproduced by the integration owner. |
| 7 | Side-effect-count divergence (results identical): a logging `tier` getter fires 2× under the original vs 3× under the refactor on starter accounts; `monthsActive` 1× vs 2× on the enterprise-below-tenure path | **Accepted — documented** in `comparison.md`. |

Integration reasoning for deferring rather than fixing: every completion criterion and every gate of the exercise is satisfied (all ten protected golden observations are plain-data and identical; `before-output.json` and `after-output.json` are byte-identical; `refactor:verify` and `verify:exercise` pass). The divergence class requires hostile inputs — per-read-varying getters, self-mutating getters, read-once Proxies — which lie beyond the golden observations that define the exercise's protected behavior. Modifying the module after the recorded first attempt would contaminate the matched-condition experiment the exercise is running (the README mandates a fresh first-attempt session for the refactor). The finding is therefore documented as a deferred follow-up: a read-parity-faithful structure would need to preserve the original's exact single-read semantics per branch (the else-if cascade's evaluation order), which the first-match-wins table does not.

## What the lane checked and found clean

All ten golden cases deep-equal including key order; 152,880 value combinations (15 tiers × 28 monthsActive × 27 latePayments × 7 overrides) spanning `undefined`/`null`/`""`, NaN, ±Infinity, −0, fractions, numeric strings, booleans, arrays, objects, Symbols, BigInts — zero divergences, including identical `TypeError` behavior on Symbol/BigInt relational comparisons; key insertion order `status, discountPercent, reason` on all five paths; fresh result objects with no shared mutable state (`PLAN_NOT_SUPPORTED` at `after.mjs:28` is module-private, spread-copied at `after.mjs:36`); function metadata identical (name, arity, constructor, prototype, `this`-binding irrelevant across five receivers); bad receivers (`undefined`, `null`, primitives, Symbol, BigInt, function) identical; `supportOverride` strictness (`1`, `"true"`, `[true]`, `1n`, `"1"` all fail `=== true`) and read exactly once on every path in both versions — structurally immune to stateful getters; null-prototype, frozen, symbol-keyed, and prototype-chain-getter input shapes identical; NaN latePayments → manual-review in both.
