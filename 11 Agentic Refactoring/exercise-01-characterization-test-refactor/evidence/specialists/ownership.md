# Specialist Ownership — Exercise 11.1

Three read-only review lanes ran in parallel as opencode general task subagents on opencode-go/glm-5.3 (the harness runs one model; subagent model selection is not exposed, so every lane inherited opencode-go/glm-5.3 — the same slug as the integration owner and both first-attempt agents). All lanes were instructed to be adversarial, to cite `file:line` for every claim against a named artifact, and to enumerate what they checked even when clean. No lane created, edited, or deleted any file inside an exercise worktree and none ran mutating Git commands; the before-attempt and behavior-parity lanes wrote scratch reproducers only under `/tmp/scratch11` and `/tmp/parity-scratch`, outside both worktrees. The integration owner re-derived every accepted claim before acting on it.

| Lane | Scope | Out of bounds (owned by others) | Write permission | Verification command | Output shape |
|---|---|---|---|---|---|
| Before-attempt review | The unconstrained attempt at `bf7b280` (branch `codex/exercise-11-01-characterization-test-refactor-before`, worktree `/tmp/ex-11-01-before`): the six README review dimensions, harness blind spots, embedded-oracle fidelity, shared-mutable-state and key-order checks | After attempt, main branch, evidence file contents beyond `before.patch` | Read-only | `node ./scripts/run-characterization-oracle.mjs` (before worktree) | Verdict + per-dimension table + `file:line` defects |
| Behavior parity | The characterization-first refactor at `550e1a2` vs the original at `52090ed`: export, inputs, result fields, exact reason strings, validation gaps, decision order; adversarial hunt for divergence classes the 49,392-variation plain-data sweep missed | Before attempt, evidence file contents | Read-only | `node ./src/rules/legacyEligibility.characterization.test.mjs` and `node ./scripts/run-characterization-oracle.mjs` (main worktree) | Verdict + per-contract-item table + `file:line` defects |
| Evidence integrity | Every committed-to-be evidence artifact vs `submission-contract.json`, `verify-refactor-submission.mjs` / `refactor-verification.mjs`, root `comparable-evidence.mjs`, README evidence list; cross-check of every SHA, hash, count, and numstat claimed in prose | Code quality of the two refactors | Read-only | `npm run refactor:verify` and `node ../../../scripts/comparable-evidence.mjs ..` (main worktree) | Verdict + per-artifact table + precise citations |

The two first-attempt agents (before: unconstrained, after: characterization-first) were implementation subagents, not review lanes; their conditions and outputs are recorded in `evidence/before.md` and `evidence/after.md`.
