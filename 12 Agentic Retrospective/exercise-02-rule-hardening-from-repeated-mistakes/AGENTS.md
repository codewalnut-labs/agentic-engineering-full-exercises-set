# Agent Routing

Before writing or changing any saved filter, setting, or preference (durable persistence work), read `.agent/persistence.md` in this directory first and follow it.

After implementing, run `npm run test:persistence` in `rule-hardening-app` and make sure it passes before finishing.

This file only routes; it holds no persistence rules itself.
