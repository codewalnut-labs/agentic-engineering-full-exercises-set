# Maintenance notes

The support-admin export predates customer workspaces and remains available for internal investigations.
The case-list service was introduced when customers gained access to the product.
Reporting work is now requested for customers. Existing integrations depend on the case-list behaviour and immutable records.

Application time must be controllable in tests. Support snapshots should not change stored data.
